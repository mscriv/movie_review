<div class="jumbotron">
  <h1> Your favorite movies reviewed</h1>
</div>

<div class="row">
  <% @movies.each do |movie| %>
    <div class="col-sm-6 col-md-3">
    <div class="thumbnail">
     <%= link_to (image_tag movie.image_url(:thumb).to_s, class: 'image'), movie %>
      </div>
      </div>
      <% end %>
  </div>



<%= link_to 'New Movie', new_movie_path %>
